const readline = require("readline")

const readAnswers = readline.createInterface({
    input: process.stdin,
    output: process.stdout
})


 readAnswers.question(`Qual o nome da candidata?`, nameCandidate => {

 }) != null ? readAnswers.console.log  :

readAnswers.question( => {
    readAnswers.question(`Qual a altura da candidata?`, higthCandidate => {
      
       }else if(nameCandidate === "FIM"){
           readAnswers.setPrompt(`O nome da candidata mais alta é: ${nameTollerCandidate}` )
       }{
        
       } 
        readAnswers.close();
    })
})